import { createTheme } from '@mui/material/styles';

const customTheme = createTheme ({
        palette: {
            mode: "light",
            primary: {
                main: "#38ccf8",
            },
            secondary: {
                main: "#7dddfc",
            },
        },
    });

    export default customTheme;